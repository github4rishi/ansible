---
- name: remove multiple dns entries
  hosts: rocky
  tasks:
  - name: delete last two dns 
    blockinfile:
      marker: "<##-- {mark} Adding nameservers -->"
      path: /etc/resolv.conf
      block: |
        nameserver: 10.10.10.10
        nameserver: 11.11.11.11
      state: absent
